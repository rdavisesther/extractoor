# MailCMH frontend is plain static HTML/CSS/JS served by the Flask backend.
# No build step, no environment variables required.
# Just run:  python app.py   (from the backend folder), then open http://localhost:5000
